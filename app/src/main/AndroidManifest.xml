<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Scoped storage: API 33+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Legacy storage: API 32 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Camera is used but not required (app works without it) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".PhotogramApplication"
        android:allowBackup="true"
        android:label="Photogram"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Magic-link / OAuth redirect handler.
                Redirect URL to register in the Supabase dashboard:  io.photogram://callback
                supabase-kt Auth plugin (scheme="io.photogram", host="callback") appends this
                URL automatically to signInWith(OTP) requests so Supabase knows where to redirect.
                singleTask launch mode ensures onNewIntent is called instead of creating a second
                MainActivity when the deep link re-opens an already-running app.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="io.photogram" android:host="callback" />
            </intent-filter>
        </activity>

    </application>

</manifest>
